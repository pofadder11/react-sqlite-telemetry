cd server
python -m venv .venv && source .venv/Scripts/activate
pip install -r requirements.txt
uvicorn server:app --reload --host 0.0.0.0 --port 8001

cd client
npm i
npm run dev
# open here -> default http://localhost:5173
